package academy.everyonecodes.java.es.stream;

import academy.everyonecodes.java.movies.Movie;

import java.util.List;
import java.util.Optional;

public class MovieParser {
/*
    public Optional<Movie> parse(String input) {
        List<String> inputIntoSeparateParts = List.of(input.split(";"));
        List<String> genres = List.of(inputIntoSeparateParts.get(1).split(":")).stream()
                .map()

        String name;
        String genre;
        int time;
        long revenue;
        try {

        } catch (NumberFormatException e) {
            e.printStackTrace();
            return Optional.empty();
        }

        return Optional.of(new Movie(country, rank, score));
    }*/
}
